# https://render.com — Blueprint: New → Blueprint → connect repo. Set GEMINI_API_KEY in the dashboard (secret).
#
# Repo layout: if Dockerfile is at the repo root (default for github.com/shahronit/data-layer-agent),
# do NOT set rootDir. Only use rootDir when the app is in a subfolder, e.g.:
#   rootDir: data-layer-agent
# Then dockerfilePath is relative to that folder (usually ./Dockerfile).
services:
  - type: web
    name: layerlens
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /api/health
    # Persistent disk for SQLite database and screenshots.
    # Data survives deploys and container restarts.
    disk:
      name: layerlens-data
      mountPath: /app/data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
      # Optional: gemini-2.5-flash is cheaper/faster than default gemini-2.5-pro
      # - key: GEMINI_MODEL
      #   value: gemini-2.5-flash
